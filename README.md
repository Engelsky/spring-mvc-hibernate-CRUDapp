# Spring MVC Hibernate CRUD App

Простое CRUD (Create, Read, Update, Delete) веб-приложение для управления списком пользователей.
Проект написан на **Java** с использованием **Spring MVC**, **Hibernate** и **Thymeleaf** (без использования Spring Boot).

## 🛠 Технологический стек

*   **Java 17**
*   **Spring MVC 5** (Конфигурация через Java classes, без `web.xml`)
*   **Hibernate 5** (ORM, работа с базой данных через `EntityManager`)
*   **MySQL 8** (База данных)
*   **Thymeleaf** (Серверный шаблонизатор)
*   **Maven** (Сборщик проекта)
*   **Tomcat 9** (Веб-сервер)
*   **HTML5/CSS3** (Простой интерфейс с использованием стилей таблиц)

## 📋 Функционал

1.  **Просмотр списка пользователей:** Вывод всех записей из БД в виде таблицы ("зебра" стилизация, [https://habr.com/ru/articles/222579](https://habr.com/ru/articles/222579)).
2.  **Добавление:** Форма создания нового пользователя.
3.  **Редактирование:** Изменение данных существующего пользователя.
4.  **Удаление:** Удаление пользователя из базы данных.
5.  **Архитектура:** Соблюден паттерн MVC (Model-View-Controller) и слои Service/DAO.

## 📂 Структура проекта

*   `config` — Конфигурация Spring (`WebConfig`, `PersistenceConfig`, `AppInit`).
*   `controller` — Обработка HTTP-запросов.
*   `service` — Бизнес-логика и управление транзакциями (`@Transactional`).
*   `dao` — Слой доступа к данным (работа с `EntityManager`).
*   `model` — JPA сущность `User`.
*   `webapp/WEB-INF/pages` — HTML шаблоны Thymeleaf.

## 🚀 Как запустить проект

### Предварительные требования
*   Установлен JDK 17+
*   Установлен Apache Tomcat 9
*   Установлен MySQL Server

### 🚀 Сборка и запуск в IntelliJ IDEA

1.  Клонируйте репозиторий или откройте проект в IDEA.
2.  Добавьте конфигурацию запуска **Tomcat Server -> Local**:
    *   Во вкладке **Deployment** добавьте артефакт: `Artifact...` -> `war exploded`.
    *   В поле **Application context** укажите `/`.
3.  Запустите сервер (**Run**).
4.  Приложение будет доступно по адресу: [http://localhost:8088/users](http://localhost:8088/users)

### 🗄 Настройка Базы Данных
Создайте пустую базу данных в MySQL. Таблицы будут созданы автоматически Hibernate'ом.

```sql
CREATE DATABASE spring_hibernate_db;

Проверьте настройки подключения в файле `src/main/resources/db.properties`. Измените `username` и `password` на ваши:

```properties
db.driver=com.mysql.cj.jdbc.Driver
db.url=jdbc:mysql://localhost:3306/spring_hibernate_db?verifyServerCertificate=false&useSSL=false&requireSSL=false&serverTimezone=UTC
db.username=root
db.password=root
